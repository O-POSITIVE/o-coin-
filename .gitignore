__pycache__/
*.pyc

# Per-node chain persistence and logs — regenerated by running node.py,
# never something to commit (also port-specific by design, see node.py).
chain_data_*.db
*.log

# Throwaway wallet/miner identities generated during local testing.
miner_wallet.txt
miner_addr.txt
